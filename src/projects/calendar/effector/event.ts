import { createEvent } from "effector";

export const moveToNextMonth = createEvent();
export const moveToPreviousMonth = createEvent();
export const moveToNextYear = createEvent();
export const moveToPreviousYear= createEvent();
export const moveToToday = createEvent();
